name: STB bump visible build token 411444e

'on':
  push:
    branches: ['main']

permissions:
  contents: write

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Advance only the System Build cache token
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('README.md')
          s = p.read_text()
          old = 'system-build-current.html?v=d80e1a8'
          new = 'system-build-current.html?v=411444e'
          if s.count(old) != 1:
              raise SystemExit(f'expected exactly one old token, found {s.count(old)}')
          s = s.replace(old, new, 1)
          if s.count(new) != 1:
              raise SystemExit('new token verification failed')
          p.write_text(s)
          PY
          git diff --check -- README.md
      - name: Commit token and remove temporary workflow
        run: |
          git rm .github/workflows/stb-bump-visible-build-411444e.yml
          git config user.name 'Scan-to-Build bounded edit'
          git config user.email 'actions@users.noreply.github.com'
          git add README.md
          git commit -m 'Point System Build button to repaired tile images'
          git push origin HEAD:main
