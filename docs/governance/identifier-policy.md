# Identifier policy (U-03 working freeze)

Record `id`: `^[A-Za-z0-9._:-]{8,128}$`. Recommended `stb:{type}:{token}`.

Fixture tokens are stable and hand-assigned so released fixtures remain immutable.

This is a working freeze for M1 fixtures, not a closed counsel decision.
