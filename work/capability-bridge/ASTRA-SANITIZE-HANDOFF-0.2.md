# Astra sanitizing pass handoff 0.2

**Run only after the current behavior/content baseline is checkpointed.**

## Scope

Sanitize language and presentation across the current candidate app branch without redesigning architecture or promoting unresolved capability.

Primary branch at handoff: `build/app-configurator-engine-0.1`.

Review together:

- `apps/stb/` current candidate behavior;
- `work/user-intake/` intake/translation material;
- `work/capability-bridge/QUALIFIED-RESOLUTION-0.1.md`;
- `work/capability-bridge/PICNIC-TABLE-CLASS-CANDIDATE-0.2.md`;
- current semantic boundaries and authority map;
- public evaluator copy only as a presentation surface, never as controlling architecture.

## Preserve exactly or semantically intact

- source/read/extracted/derived is not authority;
- user confirmation is not order/payment/allocation/release;
- qualified resolution is not release;
- production release is not machine readiness;
- machine readiness is not Cycle Start;
- Store support is not fabrication;
- unresolved means unresolved;
- no silent unit conversion, material substitution, geometry rewrite, conflict resolution, or authority promotion;
- `NO BLOOD ON WOOD`;
- current locked landing proposition unless explicitly authorized otherwise.

## Picnic-specific cleanup

The current picnic candidate intentionally carries some awkward labels such as `fixture`, `candidate`, `software-fixture`, and donor-extension language. Make the public/user copy clearer, but do not erase evidence status.

Do not turn:

- 60–216 in candidate input range into a structural, Store, machine, shipping, or fulfillment limit;
- `frame-kit` into a supported fulfillment promise;
- material preference into material identity/SKU/availability;
- geometric span into structural adequacy;
- synthetic geometry into an approved picnic-table design;
- any modeled process or cost into Store or Machine Build evidence.

## Desired result

A cold reader should understand the working app without architecture jargon, while a reviewer can still inspect the basis and see every unresolved boundary.

This is a language/consistency pass. If the pass discovers a real behavior contradiction, report it separately rather than silently fixing behavior while editing copy.
