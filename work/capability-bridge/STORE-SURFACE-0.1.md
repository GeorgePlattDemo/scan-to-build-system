# Store Surface 0.1

**Status:** empty pending mine  
**Evidence class until filled:** PLANNED  
**Store today:** Stage-2 reference pin `b40cdc60` remains the app-consumed Store

This file will list only what Store 1 may publish after each model exists:

- offering name and stock class;
- operations it will evaluate;
- answers it may return (`SUPPORTABLE` / unsupported / unresolved);
- instruction packet it may attach (machine-neutral, not servo code);
- what it must not say (order, payment, staged pickup as physical fact, measured cycle time).

A Store listing of the dimensional model plus the beginning-stage sheet model is still two offerings.
It is not a cell claim.

Cell language stays in `work/cell/` until this file can point at both offerings without invention.
